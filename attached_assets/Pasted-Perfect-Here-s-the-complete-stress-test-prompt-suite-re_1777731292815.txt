Perfect. Here's the complete stress test prompt suite — ready to run directly in your REPRO web UI, one by one.

---

# REPRO — Complete Feature Stress Test

## How to use this
Run each test case by pasting the input into REPRO's web UI. After each test, note the result in the markdown file at the end. Go in order — early tests are simple, later ones are adversarial.

---

## MODULE 1 — Entity Extraction Agent

**What you're testing:** Does Agent 1 correctly parse raw input into structured facts (actions, expected, actual, environment, frequency)?

---

**Test 1.1 — Clean, well-written bug report**
```
When I click "Add to Cart" on a product page, the cart icon shows the item was added but when I navigate to the cart page it is empty. This happens every time on Chrome desktop. Expected behavior: item should appear in cart.
```
**Pass condition:** Extracts action (click Add to Cart), expected (item in cart), actual (cart empty), environment (Chrome desktop), frequency (always)

---

**Test 1.2 — Vague, poorly written report**
```
app crashes sometimes when doing stuff in the dashboard idk why
```
**Pass condition:** Extracts what it can, flags missing fields, triggers smart questioning instead of hallucinating

---

**Test 1.3 — Non-English mixed input**
```
Login page pe click karta hoon toh 500 error aata hai. Expected tha ki dashboard open ho.
```
**Pass condition:** Either extracts correctly or gracefully handles non-English without crashing

---

**Test 1.4 — Multiple bugs in one report**
```
Two issues: First, the search bar returns no results for single-character queries. Second, the profile picture upload fails silently with no error message on Firefox.
```
**Pass condition:** Either separates into two structured outputs or clearly identifies it as multi-bug input and handles accordingly

---

**Test 1.5 — Completely empty input**
```
(submit with blank input)
```
**Pass condition:** Does not crash. Returns a validation message asking for input.

---

## MODULE 2 — GitHub Issue Ingestion

**What you're testing:** Does REPRO correctly fetch and parse a real GitHub issue URL?

---

**Test 2.1 — Simple, clean GitHub issue**
```
https://github.com/expressjs/express/issues/5520
```
**Pass condition:** Fetches title, description, comments. Builds context. Passes to pipeline.

---

**Test 2.2 — Issue with contradictory comments**
```
https://github.com/vercel/next.js/issues/49169
```
**Pass condition:** Handles contradictory comments without crashing. Filters noise and extracts signal.

---

**Test 2.3 — Closed and resolved issue**
```
https://github.com/facebook/react/issues/11538
```
**Pass condition:** Processes it correctly even though it's closed. Does not skip because of resolved status.

---

**Test 2.4 — Invalid / non-existent GitHub URL**
```
https://github.com/fakerepo/doesnotexist/issues/99999
```
**Pass condition:** Returns a clear error. Does not crash or hallucinate a bug report.

---

**Test 2.5 — GitHub URL with 50+ comments**
```
https://github.com/microsoft/vscode/issues/156543
```
**Pass condition:** Handles large comment threads. Chunking and filtering works. Does not timeout or truncate incorrectly.

---

## MODULE 3 — Stack Trace to Reproduction Mapping

**What you're testing:** Does REPRO parse a stack trace, identify the failure origin, and generate reproduction steps?

---

**Test 3.1 — JavaScript TypeError**
```
TypeError: Cannot read properties of undefined (reading 'map')
    at ProductList (ProductList.jsx:24)
    at renderWithHooks (react-dom.development.js:14985)
    at mountIndeterminateComponent (react-dom.development.js:17811)
    at beginWork (react-dom.development.js:19049)
```
**Pass condition:** Identifies failure at ProductList.jsx line 24. Generates reproduction involving undefined data being passed to a list component.

---

**Test 3.2 — Python backend traceback**
```
Traceback (most recent call last):
  File "app/routes/auth.py", line 47, in login
    user = db.session.query(User).filter_by(email=email).first()
  File "sqlalchemy/orm/query.py", line 1667, in first
    return self.limit(1)._iter().first()
sqlalchemy.exc.OperationalError: (psycopg2.OperationalError) SSL connection has been closed unexpectedly
```
**Pass condition:** Identifies database SSL connection failure. Generates reproduction involving DB connection state.

---

**Test 3.3 — Stack trace with no clear origin**
```
Error: ECONNREFUSED 127.0.0.1:5432
    at TCPConnectWrap.afterConnect [as oncomplete] (node:net:1187:16)
```
**Pass condition:** Identifies connection refused error. Asks clarifying question about whether DB server is running rather than hallucinating a reproduction.

---

**Test 3.4 — Stack trace + bug description combined**
```
Users report being logged out randomly.

Stack trace from Sentry:
JsonWebTokenError: invalid signature
    at /app/middleware/auth.js:23:15
    at Layer.handle [as handle_request] (express/lib/router/layer.js:95:5)
```
**Pass condition:** Combines both inputs. Generates richer reproduction than either alone would produce.

---

**Test 3.5 — Minified/obfuscated stack trace**
```
TypeError: a is not a function
    at t.<anonymous> (main.abc123.js:1:4521)
    at e (vendor.abc123.js:1:892)
```
**Pass condition:** Identifies that the trace is minified. Either asks for source maps or explains the limitation clearly.

---

## MODULE 4 — Runnable Test Code Generation

**What you're testing:** Does REPRO generate syntactically correct, runnable test code in the right framework?

---

**Test 4.1 — Backend API bug → should generate pytest**
```
POST /api/users/login returns 200 even when the password is wrong. Expected: 401 Unauthorized.
```
**Pass condition:** Generates a pytest test that sends a POST request with wrong credentials and asserts 401 status code. Includes imports and setup.

---

**Test 4.2 — Frontend UI bug → should generate Cypress**
```
On mobile viewport (375px), the navigation hamburger menu opens but clicking any menu item does not close it or navigate.
```
**Pass condition:** Generates a Cypress test that sets viewport to 375px, opens hamburger, clicks a menu item, asserts navigation occurred.

---

**Test 4.3 — API rate limiting bug → should generate Postman / REST test**
```
The /api/search endpoint does not enforce rate limiting. Sending 1000 requests in under a second returns 200 for all of them.
```
**Pass condition:** Generates a test that fires multiple rapid requests and asserts rate limit response (429) is returned.

---

**Test 4.4 — Ambiguous context (no clear framework)**
```
Button doesn't work.
```
**Pass condition:** Asks clarifying questions about the stack (frontend/backend, framework) instead of generating wrong test code.

---

## MODULE 5 — Live Pipeline Visualization

**What you're testing:** Does the UI correctly show each agent activating, processing, and completing in sequence?

---

**Test 5.1 — Normal flow visualization**
```
Clicking "Save" on the profile edit page does nothing. No network request is made. No error shown.
```
**Pass condition:** UI shows Agent 1 → 2 → 3 → 4 activating in sequence. Each stage shows a processing state before completing. No stage skipped.

---

**Test 5.2 — Visualization during smart questioning pause**
```
App is slow sometimes.
```
**Pass condition:** Pipeline visibly pauses at the confidence check stage and shows the questioning state — does not jump straight to output.

---

**Test 5.3 — Rapid re-submission**
Submit a bug report. Immediately submit another before the first completes.

**Pass condition:** Either queues correctly or resets cleanly. Does not show mixed state from two runs simultaneously.

---

## MODULE 6 — Confidence-Driven Smart Questioning

**What you're testing:** Does REPRO ask targeted questions when data is insufficient instead of hallucinating?

---

**Test 6.1 — Missing environment**
```
The checkout button is not working.
```
**Pass condition:** Asks about browser, device, user account type, or network conditions — not a generic "tell me more."

---

**Test 6.2 — Missing expected behavior**
```
When I submit the form, something weird happens.
```
**Pass condition:** Asks specifically what the expected outcome was — not generic clarification.

---

**Test 6.3 — Missing frequency**
```
The dashboard sometimes shows incorrect data.
```
**Pass condition:** Asks about when this happens — specific conditions, timing, user type — to nail down the intermittent nature.

---

**Test 6.4 — Complete information (should NOT ask questions)**
```
On Safari 16.4 on iOS 16, when a logged-in user with an expired session token navigates to /dashboard, they are redirected to /login but then immediately redirected back to /dashboard without re-authenticating. This happens every time.
```
**Pass condition:** Does NOT ask questions. Goes directly to hypothesis generation since all fields are present.

---

## MODULE 7 — Multi-Bug Correlation Engine

**What you're testing:** Does REPRO detect when a new bug is structurally similar to a previously processed one?

---

**Test 7.1 — Submit a bug, then submit a similar one**

First submit:
```
On Firefox, when a user is idle for 30 minutes, the session expires but the UI does not redirect them to login. They see a blank page instead.
```

Then submit:
```
Safari users are seeing a white screen after being inactive for some time. No redirect happens.
```
**Pass condition:** Second submission surfaces similarity to first. References it with a similarity score or note.

---

**Test 7.2 — Completely unrelated bug (should NOT trigger correlation)**
```
CSV export downloads an empty file when the dataset has more than 10,000 rows.
```
**Pass condition:** No false correlation triggered. Output is independent.

---

## MODULE 8 — Environment Diff Detector

**What you're testing:** Does REPRO identify which environment difference is most likely causing the bug?

---

**Test 8.1 — Classic staging vs production diff**
```
Environment 1 (works): Node.js 18.12, PostgreSQL 14, Redis 6.2, Ubuntu 22.04, JWT_SECRET set
Environment 2 (fails): Node.js 20.10, PostgreSQL 15, Redis 7.0, Ubuntu 22.04, JWT_SECRET set

Bug: Authentication fails only in Environment 2
```
**Pass condition:** Identifies Node.js version difference or PostgreSQL 15 breaking change as most likely cause.

---

**Test 8.2 — Browser environment diff**
```
Works on: Chrome 120, Windows 11, 1920x1080, fast network
Fails on: Safari 16, macOS Ventura, 375x812, slow 3G

Bug: Image gallery does not load
```
**Pass condition:** Identifies browser difference (Safari CSS/JS compatibility) or network speed as primary suspect.

---

## MODULE 9 — Reproduction Session Collaboration

**What you're testing:** Does the collaborative session feature work correctly for multiple users?

---

**Test 9.1 — Basic session sharing**
Open REPRO in two different browser tabs. Submit a bug in Tab 1. Check if Tab 2 can join and see the same pipeline state.

**Pass condition:** Tab 2 sees the running pipeline. Both tabs are in sync.

---

**Test 9.2 — Annotation from second user**
In the shared session, have the second user mark a hypothesis as "Verified" or "Incorrect."

**Pass condition:** First user sees the annotation in real time. Pipeline updates accordingly.

---

## MODULE 10 — Bug Severity Classifier

**What you're testing:** Does REPRO correctly classify bug severity based on impact signals?

---

**Test 10.1 — Clearly critical bug**
```
All users are unable to log in. The /api/auth/login endpoint returns 500 for every request since the last deployment 20 minutes ago.
```
**Pass condition:** Classified as Critical. Reasoning mentions all-users impact and production scope.

---

**Test 10.2 — Low severity cosmetic bug**
```
The tooltip on the settings page icon appears 2px misaligned on Windows Chrome.
```
**Pass condition:** Classified as Low. Reasoning mentions cosmetic nature and limited user impact.

---

**Test 10.3 — Ambiguous severity**
```
Sometimes the email notification is sent twice.
```
**Pass condition:** Classified as Medium or asks clarifying questions about frequency and user impact before classifying.

---

## MODULE 11 — Flaky Test Detector

**What you're testing:** Does REPRO identify why a test is flaky and explain the root cause?

---

**Test 11.1 — Timing-dependent flaky test**
```
Test: test_user_session_expires
This test passes 7 out of 10 runs. It creates a user session, waits 100ms, then asserts the session is still valid.
```
**Pass condition:** Identifies that 100ms wait is too close to a timeout boundary — race condition between test timing and session logic.

---

**Test 11.2 — Data-dependent flaky test**
```
Test: test_search_returns_results
Passes when run alone. Fails when run after test_delete_all_products in the same suite.
```
**Pass condition:** Identifies test order dependency — shared database state not being reset between tests.

---

## MODULE 12 — Reproduction Audit Trail

**What you're testing:** Does REPRO log every decision with reasoning that is reviewable?

---

**Test 12.1 — Audit trail completeness**

Run any bug through the full pipeline. Then access the audit trail.

**Pass condition:** Trail shows every hypothesis generated, which ones were eliminated and why, which steps were validated, and what assumptions were made. Nothing is missing.

---

**Test 12.2 — Audit trail export**

Export the audit trail from Test 12.1.

**Pass condition:** Exports cleanly as a readable file (JSON or PDF or markdown). All decisions are present.

---

## MODULE 13 — Natural Language to Test (NL2Test)

**What you're testing:** Does NL2Test generate correct test cases from plain English descriptions without a bug report?

---

**Test 13.1 — Simple NL test request**
```
Test what happens when a user tries to access /admin without being logged in.
```
**Pass condition:** Generates a test that hits /admin without auth headers and asserts 401 or redirect to login.

---

**Test 13.2 — Complex multi-condition NL test**
```
Test that a user with a free plan cannot access premium features, but a user with a paid plan can, and that switching plans mid-session reflects immediately.
```
**Pass condition:** Generates two user setup blocks, tests access for each, and asserts real-time plan switch behavior.

---

## MODULE 14 — Bug Pattern Recognition

**What you're testing:** Does REPRO correctly match bugs to known failure patterns?

---

**Test 14.1 — Race condition**
```
When two users update the same record at exactly the same time, one update is silently lost.
```
**Pass condition:** Pattern identified as Race Condition / Write Conflict. Suggests optimistic locking or transaction isolation.

---

**Test 14.2 — Null reference**
```
App crashes when a user who has never set a profile picture tries to view their profile.
```
**Pass condition:** Pattern identified as Null Reference / Missing Default. Points to unguarded property access.

---

**Test 14.3 — Auth failure pattern**
```
Admin users are being logged out every 15 minutes even though the session timeout is set to 24 hours.
```
**Pass condition:** Pattern identified as Auth / Token Expiry mismatch. Suggests JWT exp claim vs server-side session config discrepancy.

---

---

# REPRO Stress Test Report Template

Copy this into a `.md` file, fill it in as you run each test.

```markdown
# REPRO — Full Feature Stress Test Report
**Date:** 
**Tester:** 
**Build version / commit:**

---

## Test Results Summary

| Test ID | Feature | Input Type | Pass / Fail | Notes |
|---------|---------|------------|-------------|-------|
| 1.1 | Entity Extraction | Clean text | | |
| 1.2 | Entity Extraction | Vague text | | |
| 1.3 | Entity Extraction | Mixed language | | |
| 1.4 | Entity Extraction | Multi-bug | | |
| 1.5 | Entity Extraction | Empty input | | |
| 2.1 | GitHub Ingestion | Clean issue | | |
| 2.2 | GitHub Ingestion | Contradictory comments | | |
| 2.3 | GitHub Ingestion | Closed issue | | |
| 2.4 | GitHub Ingestion | Invalid URL | | |
| 2.5 | GitHub Ingestion | 50+ comments | | |
| 3.1 | Stack Trace | JS TypeError | | |
| 3.2 | Stack Trace | Python traceback | | |
| 3.3 | Stack Trace | No clear origin | | |
| 3.4 | Stack Trace | Combined input | | |
| 3.5 | Stack Trace | Minified trace | | |
| 4.1 | Test Generation | pytest | | |
| 4.2 | Test Generation | Cypress | | |
| 4.3 | Test Generation | REST/Postman | | |
| 4.4 | Test Generation | Ambiguous | | |
| 5.1 | Visualization | Normal flow | | |
| 5.2 | Visualization | Pause on questioning | | |
| 5.3 | Visualization | Rapid re-submission | | |
| 6.1 | Smart Questioning | Missing environment | | |
| 6.2 | Smart Questioning | Missing expected | | |
| 6.3 | Smart Questioning | Missing frequency | | |
| 6.4 | Smart Questioning | Complete input | | |
| 7.1 | Correlation Engine | Similar bugs | | |
| 7.2 | Correlation Engine | Unrelated bug | | |
| 8.1 | Env Diff Detector | Server diff | | |
| 8.2 | Env Diff Detector | Browser diff | | |
| 9.1 | Collaboration | Session sharing | | |
| 9.2 | Collaboration | Annotation sync | | |
| 10.1 | Severity Classifier | Critical | | |
| 10.2 | Severity Classifier | Low | | |
| 10.3 | Severity Classifier | Ambiguous | | |
| 11.1 | Flaky Test Detector | Timing-dependent | | |
| 11.2 | Flaky Test Detector | Data-dependent | | |
| 12.1 | Audit Trail | Completeness | | |
| 12.2 | Audit Trail | Export | | |
| 13.1 | NL2Test | Simple request | | |
| 13.2 | NL2Test | Complex multi-condition | | |
| 14.1 | Pattern Recognition | Race condition | | |
| 14.2 | Pattern Recognition | Null reference | | |
| 14.3 | Pattern Recognition | Auth failure | | |

---

## REPRO vs ChatGPT — Comparative Analysis

### Test: Auth bug (JWT invalid signature)

**Input given to both:**
```
Users are being randomly logged out. Stack trace:
JsonWebTokenError: invalid signature
    at /app/middleware/auth.js:23:15
```

**ChatGPT output:**
- Unstructured paragraph
- One possible cause mentioned
- No test code
- No validation
- No confidence score
- No visual output

**REPRO output:**
- Structured JSON entity extraction
- 3 hypotheses generated (key rotation, env mismatch, token tampering)
- Step validator eliminated 1, retained 2
- pytest test case generated and runnable
- Confidence scores: 78% and 61%
- Visual execution flow diagram rendered
- Audit trail: full decision log available

**Verdict:** REPRO produced an engineering artifact. ChatGPT produced a suggestion.

---

### Test: Vague report ("app crashes sometimes")

**ChatGPT output:**
- Listed 5–8 generic possible causes
- Suggested checking logs (which the user already has)
- No structure, no next step

**REPRO output:**
- Identified missing fields: environment, frequency, trigger action
- Generated 3 targeted questions
- Did not hallucinate reproduction steps
- Paused pipeline visibly — showed reasoning transparency

**Verdict:** REPRO knew what it didn't know. ChatGPT guessed.

---

### Test: Multi-Bug Input

**ChatGPT output:**
- Mixed both bugs into one response
- Addressed neither fully

**REPRO output:**
- Detected two separate bug entities
- Processed each through independent pipeline runs
- Generated separate test cases for each

**Verdict:** REPRO enforced structure. ChatGPT produced noise.

---

## Overall Findings

**Total tests run:**
**Passed:**
**Failed:**
**Partial:**

**Critical failures (if any):**

**Strongest features:**

**Features needing improvement before demo:**

**Recommended demo test case (the one that shows REPRO best):**
```

---

That's 43 test cases across 14 feature modules plus the full comparison report template. Run them top to bottom — the failures will tell you exactly what needs fixing before the hackathon. Want me to also write the demo script for what to run live on stage?